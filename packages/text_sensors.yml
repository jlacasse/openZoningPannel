text_sensor:
  - platform: template
    name: "Zone 1 State"
    id: z1_state_text
    update_interval: never
    lambda: |-
      switch (id(z1_state)) {
        case ZONE_OFF:
          return {"Off"};
        case ZONE_FAN_ONLY:
          return {"Fan Only"};
        case ZONE_COOLING_STAGE1:
          return {"Cooling Stage 1"};
        case ZONE_COOLING_STAGE2:
          return {"Cooling Stage 2"};
        case ZONE_HEATING_STAGE1:
          return {"Heating Stage 1"};
        case ZONE_HEATING_STAGE2:
          return {"Heating Stage 2"};
        case ZONE_ERROR:
          return {"ERROR"};
        default:
          return {"Unknown"};
      }

  - platform: template
    name: "Zone 2 State"
    id: z2_state_text
    update_interval: never
    lambda: |-
      switch (id(z2_state)) {
        case ZONE_OFF:
          return {"Off"};
        case ZONE_FAN_ONLY:
          return {"Fan Only"};
        case ZONE_COOLING_STAGE1:
          return {"Cooling Stage 1"};
        case ZONE_COOLING_STAGE2:
          return {"Cooling Stage 2"};
        case ZONE_HEATING_STAGE1:
          return {"Heating Stage 1"};
        case ZONE_HEATING_STAGE2:
          return {"Heating Stage 2"};
        case ZONE_ERROR:
          return {"ERROR"};
        default:
          return {"Unknown"};
      }

  - platform: template
    name: "Zone 3 State"
    id: z3_state_text
    update_interval: never
    lambda: |-
      switch (id(z3_state)) {
        case ZONE_OFF:
          return {"Off"};
        case ZONE_FAN_ONLY:
          return {"Fan Only"};
        case ZONE_COOLING_STAGE1:
          return {"Cooling Stage 1"};
        case ZONE_COOLING_STAGE2:
          return {"Cooling Stage 2"};
        case ZONE_HEATING_STAGE1:
          return {"Heating Stage 1"};
        case ZONE_HEATING_STAGE2:
          return {"Heating Stage 2"};
        case ZONE_ERROR:
          return {"ERROR"};
        default:
          return {"Unknown"};
      }

  - platform: template
    name: "Zone 4 State"
    id: z4_state_text
    update_interval: never
    lambda: |-
      switch (id(z4_state)) {
        case ZONE_OFF:
          return {"Off"};
        case ZONE_FAN_ONLY:
          return {"Fan Only"};
        case ZONE_COOLING_STAGE1:
          return {"Cooling Stage 1"};
        case ZONE_COOLING_STAGE2:
          return {"Cooling Stage 2"};
        case ZONE_HEATING_STAGE1:
          return {"Heating Stage 1"};
        case ZONE_HEATING_STAGE2:
          return {"Heating Stage 2"};
        case ZONE_ERROR:
          return {"ERROR"};
        default:
          return {"Unknown"};
      }

  - platform: template
    name: "Zone 5 State"
    id: z5_state_text
    update_interval: never
    lambda: |-
      switch (id(z5_state)) {
        case ZONE_OFF:
          return {"Off"};
        case ZONE_FAN_ONLY:
          return {"Fan Only"};
        case ZONE_COOLING_STAGE1:
          return {"Cooling Stage 1"};
        case ZONE_COOLING_STAGE2:
          return {"Cooling Stage 2"};
        case ZONE_HEATING_STAGE1:
          return {"Heating Stage 1"};
        case ZONE_HEATING_STAGE2:
          return {"Heating Stage 2"};
        case ZONE_ERROR:
          return {"ERROR"};
        default:
          return {"Unknown"};
      }

  - platform: template
    name: "Zone 6 State"
    id: z6_state_text
    update_interval: never
    lambda: |-
      switch (id(z6_state)) {
        case ZONE_OFF:
          return {"Off"};
        case ZONE_FAN_ONLY:
          return {"Fan Only"};
        case ZONE_COOLING_STAGE1:
          return {"Cooling Stage 1"};
        case ZONE_COOLING_STAGE2:
          return {"Cooling Stage 2"};
        case ZONE_HEATING_STAGE1:
          return {"Heating Stage 1"};
        case ZONE_HEATING_STAGE2:
          return {"Heating Stage 2"};
        case ZONE_ERROR:
          return {"ERROR"};
        default:
          return {"Unknown"};
      }
